# Copy to backend/.env and fill in. backend/.env is gitignored — never commit a real key.
# Without a key the app runs fully local and deterministic (all evals pass offline).
OPENAI_API_KEY=
# OPENAI_MODEL=gpt-4o-mini
# REALDOOR_LLM_ASSIST=1
# REALDOOR_LLM_EXPLAIN=1
# REALDOOR_LLM_CROSSCHECK=0
